// Случайная задержка появления (между 0 и 2 секунды)
seedRandom(index, true);
delay = random(0, 2);

// Время появления
appearTime = inPoint + delay;

// Сколько длится сама анимация масштаба
duration = 0.5; // можно увеличить для плавности

// Время прошедшее с момента появления
t = time - appearTime;

// Плавный переход от 0 до 100 по всем осям
val = ease(t, 0, duration, 0, 100);

// Масштаб в 3D: X, Y, Z
[val, val, val];
